export default function Home() {
  return (
    <main style={{maxWidth:960, margin:'80px auto', padding:'0 16px'}}>
      <h1 style={{fontSize:40, fontWeight:800, marginBottom:12}}>Signature M</h1>
      <p style={{opacity:.85, lineHeight:1.6}}>
        If you can read this, the Next.js <code>app/</code> directory has been detected and your deployment is working.
      </p>
      <p style={{marginTop:24, opacity:.75}}>
        Next step: we’ll swap this minimal shell for the full site once this is live.
      </p>
    </main>
  );
}
